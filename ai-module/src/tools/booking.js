/**
 * booking.js — MCP tool definition for individual booking creation.
 *
 * This tool wraps POST /api/bookings/individual and requires an auth
 * token from the session store (set after OTP verification in Step 3).
 *
 * Implemented in Step 4.
 */

// TODO: Step 4 — implement create_individual_booking
